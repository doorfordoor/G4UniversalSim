下面是基于 [IMPLEMENTATION_AND_FEASIBILITY_AUDIT.md](D:/Geant4/G4UniversalSim/docs/IMPLEMENTATION_AND_FEASIBILITY_AUDIT.md) 和当前源码状态整理的后续开发计划。此计划最初用于规划任务；Round 0、Round 1、Round 2 已完成后，相关 P1 项的状态已在本文中更新为“已完成/可回归”。

**总体排序**

| Priority | 功能                                       | 是否现在做 | 核心原因                                               |
| -------- | ------------------------------------------ | ---------: | ------------------------------------------------------ |
| P0       | `run_gdml.mac` 安全修正、文档/测试误导清理 |         是 | 防止用户直接运行已知失败 macro                         |
| P1       | `OutputMessenger`                          |       已完成 | Round 1 已注册最小 `/AIHL/output/...` 命令集合 |
| P1       | `AppMessenger` / `DetectorMessenger` 注册  |       已完成 | Round 1 已默认注册，文档保留初始化前后限制 |
| P1       | `trap` / `trapezoid`                       |       已完成 | Round 2 已接入 `G4Trd/G4Trap` |
| P1       | `layered_device copyNo/metadata`           |       已完成 | Round 2 已透传 `copyNo`、`metadata.*` 和未知 key |
| P1/P2    | 多线程输出安全                             | 是，分阶段 | 当前 MT CSV 有 race 风险，影响数据可信度               |
| P2       | `volume_fraction`                          |     稍后做 | 物理含义清楚后可实现，需密度换算                       |
| P2       | GDML 真导入                                |     稍后做 | 价值高，但架构接入中等复杂                             |
| P2       | `DoseScorer` / `FluenceScorer`             |     稍后做 | 需要明确物理定义和输出格式                             |
| P3       | STL import                                 |       推迟 | 外部依赖/mesh 稳定性风险高                             |
| P3       | Qt UI                                      |       推迟 | 当前 CLI/macro 尚需稳定                                |
| P3       | `ParticleAggregator`                       |       推迟 | 依赖输出/MT merge 稳定                                 |
| P3       | advanced biasing                           |       推迟 | 高物理风险，应先稳定 XS biasing                        |
| P3       | 真实 LET                                   |       推迟 | LET 定义和 validation 先行                             |

**功能明细**

| 功能                             | 当前实现状态                                         | 调用链断点                                                   | 是否误导 | Geant4 可行性                | 是否现在做 | 预计修改文件                                                 | 最小实现范围                                                 | 验证方法                                                     |
| -------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------ | -------- | ---------------------------- | ---------: | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `run_gdml.mac` 安全修正          | macro 存在且含 `/run/initialize`，但 GDML 后端未实现 | `VolumeBuilder::CreateSolid()` 抛错                          | 高       | 可行，但当前未接             |         是 | `macros/run_gdml.mac`, docs                                  | 注释或移除 `/run/initialize`，标成 parser-only demo          | 执行 macro 不触发失败，或文档明确预期失败                    |
| `OutputMessenger`                | Round 1 已实现并注册最小命令集合                     | 无 P1 断点；后续只剩运行中状态保护和 MT 语义                 | 低       | Straightforward              |       已完成 | `include/Output/OutputMessenger.hh`, `src/Output/OutputMessenger.cc`, `SimulationManager.*` | 已有 `/AIHL/output/setDir`, `print`, `flush`, `close`, `setThreadSuffix` | `/control/manual` 可见；macro 设置输出目录生效               |
| `AppMessenger` 注册              | Round 1 已由 `SimulationManager` 持有并注册           | 无 P1 断点                                                   | 低       | Straightforward              |       已完成 | `SimulationManager.hh/.cc`                                   | 默认注册 `/AIHL/app/...`，限制初始化后危险命令               | `/control/manual` 可见；命令执行无 unknown                   |
| `DetectorMessenger` 注册         | Round 1 已由 `DetectorConstruction` 持有并注册        | 无 P1 断点                                                   | 低       | Straightforward              |       已完成 | `DetectorConstruction.hh/.cc`                                | detector 持有 messenger；注册诊断命令                        | `/AIHL/detector/printSensitiveVolumes` 可用                  |
| `trap/trapezoid`                 | Round 2 已接入 `G4Trd/G4Trap`                        | 无 P1 断点；`trap` 参数仍需满足 Geant4 solid 约束             | 低       | Straightforward              |       已完成 | `GeometryTypes.hh`, `GeometryConfig.cc`, `VolumeBuilder.*`, docs/tests | `trd/trapezoid` 使用 `G4Trd`，`trap` 使用完整 `G4Trap`       | 新 geometry macro `/run/initialize` 成功，overlap check 通过 |
| `layered_device copyNo/metadata` | Round 2 已支持 copyNo 与 metadata passthrough        | 无 P1 断点；metadata 仍不自动输出到 CSV                      | 低       | Straightforward              |       已完成 | `LayeredDeviceTemplate.cc`, docs/tests                       | 读取 `copyNo`，未知 `metadata.*` 存入 `userProperties`       | 检查 placement copyNo；打印 tree/registry                    |
| 多线程输出                       | `_t0` 后缀存在，但 manager 共享且 threadId 固定 0    | `ActionInitialization` 共享 `OutputManager`/`ScoringManager` | 高       | 可行                         | 是，分阶段 | `ActionInitialization.*`, `RunAction.*`, `OutputManager.*`, `ScoringManager.*` | 第一阶段强制文档警告；第二阶段 per-thread output；第三阶段 merge | `--threads 2` 生成 `_t0/_t1` 且无 CSV 损坏                   |
| `volume_fraction`                | parser 可识别，factory 抛异常                        | `MaterialFactory::BuildCustomMaterial()`                     | 中       | 可行，中等                   |         P2 | `MaterialFactory.cc`, `MaterialCommandParser`, docs/tests    | 仅允许 sub-material 体积分数；用密度换算质量分数             | 单元/宏测试构建混合材料，质量分数求和为 1                    |
| GDML 真导入                      | placeholder                                          | `VolumeBuilder` 未调用 `G4GDMLParser`                        | 高       | 可行，中等                   |         P2 | `GDMLTemplate`, `GeometryManager`, `DetectorConstruction`, CMake/docs | world replacement 模式；注册 GDML logical volumes            | 小 GDML fixture 初始化成功，SD/bias volume 名称映射成功      |
| `DoseScorer`                     | no-op stub                                           | scorer 计算层                                                | 中       | 可行                         |         P2 | `DoseScorer.cc`, `ScoringManager`, `OutputManager`           | per-volume `dose = edep / mass`，定义单位和输出              | 单 box 已知 edep/mass 对比                                   |
| `FluenceScorer`                  | no-op stub                                           | scorer 计算层                                                | 中       | 可行但需定义                 |         P2 | `FluenceScorer.cc`, docs/tests                               | 先实现 track-length fluence，明确不是 surface fluence        | 固定几何 track length smoke test                             |
| 真实 LET                         | 只消费 `HitRecord::LETcalc`                          | 无 LET 计算来源                                              | 高       | 可行但高风险                 |         否 | 后续 `LETScorer`, `SensitiveDetector`                        | 先写定义文档，不先实现                                       | analog/文献基准验证后再做                                    |
| STL                              | 无 parser/backend                                    | 入口不存在                                                   | 低/中    | 需外部依赖或自研 mesh parser |         否 | future                                                       | 暂不做，推荐 STL->GDML 外部转换                              | 不进入当前验收                                               |
| Qt UI                            | 无项目级 Qt target                                   | CMake/源码无 GUI                                             | 中       | 可行但复杂                   |         否 | future 独立 target                                           | 暂不做                                                       | 不进入当前验收                                               |
| `ParticleAggregator`             | 空类                                                 | 未接入 scoring/output                                        | 低       | Straightforward              |         否 | future                                                       | 等 MT/output 稳定后做                                        | 不进入当前验收                                               |
| advanced biasing                 | 只有 XS biasing 可用                                 | 无 parser/backend                                            | 高       | 可行但高复杂/高物理风险      |         否 | future                                                       | 先不做，稳定 XS biasing                                      | analog vs biased validation 后再规划                         |

**Codex 任务轮次**

**Round 0: P0 安全清理**

任务清单：
- 修正 `macros/run_gdml.mac`，避免默认执行必然失败的 `/run/initialize`。
- 检查测试配置中是否出现 `volume_fraction`、GDML 真运行、advanced biasing 等仍未实现或高风险功能；`trd/trapezoid/trap` 与最小 `/AIHL/output/...` 已在后续 Round 中可用。
- 更新相关文档措辞，保证“placeholder/stub/not registered”明确。

验收标准：
- `run_gdml.mac` 不会在用户直接执行时误触发 GDML 构建失败。
- 文档不再暗示 GDML/STL/advanced biasing 已可运行；`OutputMessenger` 已在 Round 1 后按最小命令集合标为可用。
- 不修改核心源码。

**Round 1: P1 命令面补齐（已完成）**

任务清单：
- 实现并注册 `OutputMessenger` 最小命令集合。
- 注册 `AppMessenger`。
- 让 `DetectorConstruction` 持有并注册 `DetectorMessenger`。
- 更新 `COMMAND_REFERENCE.md`。

验收标准：
- `/control/manual` 可见 `/AIHL/output/...`、`/AIHL/app/...`、`/AIHL/detector/...`。
- `/AIHL/output/setDir` 在 `/run/initialize` 前生效。
- `/AIHL/detector/printSensitiveVolumes` 在 geometry 构建后可输出。
- 初始化后危险命令有明确限制或 warning。

**Round 2: P1 几何一致性（已完成）**

任务清单：
- 实现 `trap` / `trapezoid`，建议先 `G4Trd` 简化参数，再完整 `G4Trap`。
- `layered_device` 支持 `copyNo` 和 `metadata.*` 透传。
- 新增最小 geometry 测试配置和 macro。

验收标准：
- `shape = trap` 或推荐 alias 能 `/run/initialize` 成功。
- `layered_device` 中 `copyNo` 传入 `G4PVPlacement`。
- `metadata.*` 至少进入 `VolumeNode::userProperties`，文档说明当前是否输出。

**Round 3: P1/P2 多线程输出安全**

任务清单：
- 明确短期策略：per-thread files + explicit merge。
- 改造 worker 侧 `OutputManager`/`ScoringManager` 生命周期，避免共享写。
- 正确设置 thread id，不再所有 worker 写 `_t0`。
- 保留或增强 `merge_outputs.py` / `mergeHistograms` 使用说明。

验收标准：
- `--threads 2` 至少生成不同线程后缀文件。
- hits/event edep/histogram 不发生并发写同一文件。
- 单线程输出结果保持兼容。
- 文档说明是否已自动 merge；若未自动，给出明确命令。

**Round 4: P2 材料与 GDML**

任务清单：
- 实现 `volume_fraction`，仅支持 material components，按密度换算为 mass fraction。
- 实现 GDML world replacement：`G4GDMLParser::Read()` + `GetWorldVolume()`。
- 建立 GDML logical volume 名称映射到 `GeometryRegistry`。
- 支持 GDML `sensitive_volumes` / `bias_volumes` 的最小名称匹配。

验收标准：
- `volume_fraction` 材料构建成功，密度/质量分数校验正确。
- 一个最小 GDML 文件可 `/run/initialize`。
- GDML 中指定 sensitive volume 后可产生 hits。
- GDML 不再走 placeholder `VolumeBuilder` 抛错路径。

**Round 5: P2 Dose / Fluence**

任务清单：
- `DoseScorer` 实现明确的 `edep / mass`。
- `FluenceScorer` 先实现 track-length fluence，并在文档中避免和 surface fluence 混淆。
- 输出 per-volume/run summary CSV。
- 增加简单验证宏。

验收标准：
- `DoseScorer` 不再 no-op，有稳定输出。
- `FluenceScorer` 输出字段名明确包含定义，例如 `track_length_fluence`。
- 文档列出单位、定义和限制。
- 多线程模式下遵守 Round 3 的输出策略。

**不建议现在做**

- STL import：mesh 质量、材料/层级缺失、导航稳定性风险高；先做 GDML。
- Qt UI：当前应优先稳定 CLI/macro、输出、几何和 scoring。
- `ParticleAggregator`：等输出格式和 MT merge 稳定后再做。
- advanced biasing：importance/weight-window/splitting/Russian roulette 需要 parallel geometry/cell importance/weight validation，不应混进当前 XS biasing。
- 真实 LET：必须先定义 track-averaged / dose-averaged、restricted / unrestricted 等物理口径，并准备 validation。当前不要把 `LETScorer` 从 stub 直接升级成“真实 LET”。

## P0：立即做，目标是“项目不误导用户”

这一阶段不追求新功能，只修正危险示例、文档和测试入口。

### P0.1 修正 `run_gdml.mac`

审计指出，`run_gdml.mac` 注释说不要 initialize，但实际包含 `/run/initialize`，这会让用户直接运行失败。GDML 当前只是 placeholder，没有 `G4GDMLParser` 后端。

建议：

```
1. 删除 run_gdml.mac 中的 /run/initialize 和 /run/beamOn；
2. 或把文件改名为 run_gdml_placeholder.mac；
3. 在 macro 顶部明确写：
   当前 GDML import 未接入 G4GDMLParser，不能运行初始化。
```

------

### P0.2 更新未实现功能说明

必须明确标注以下内容：

```
volume_fraction：预留，不能运行；
GDML：placeholder，不能 /run/initialize；
STL：未实现；
trd/trapezoid/trap：Round 2 已实现，trap 参数仍需满足 Geant4 约束；
LET/Dose/Fluence：stub 或未验证；
/AIHL/output/...：Round 1 已注册最小命令，hits/scoring 开关仍归 /AIHL/scoring；
App/Detector messenger：Round 1 已默认注册；
多线程 CSV：当前不保证 race-free；
高级 biasing：future work。
```

这些结论都在审计报告中被列为 mismatch 或 avoid-for-now 项。

------

### P0.3 完成 XS biasing 验证计划

你刚完成 biasing XS 修改，所以现在应该补：

```
analog run
biased run
相同 geometry / source / physics / seed
比较 raw_edep、weighted_edep、process occurrence、hit weight
```

审计也明确写到：XS process-level biasing 是否确实改变目标 process occurrence，需要 analog vs biased run 对比验证。

建议新增：

```
macros/run_biasing_analog.mac
macros/run_biasing_process_level.mac
config/biasing/xs_process_bias.ini
docs/BIASING_VALIDATION.md
```

------

## P1：近期优先实现，低风险且收益高

P1 是你接下来最适合让 Codex 逐个实现的阶段。

------

## P1.1 实现 `OutputMessenger`

Round 1 已完成：`OutputMessenger` 继承 `G4UImessenger`，默认注册 `/AIHL/output/...` 最小命令集合。下面保留原始最小命令目标，作为回归清单。

建议最小命令：

```
/AIHL/output/setDir <dir>
/AIHL/output/print
/AIHL/output/flush
/AIHL/output/close
/AIHL/output/setThreadSuffix <true|false>
```

不要在这里加：

```
/AIHL/output/enableHits
/AIHL/output/enableScoring
```

这些仍然归 `/AIHL/scoring/...` 管。

------

## P1.2 注册 `AppMessenger` 和 `DetectorMessenger`

Round 1 已完成：`AppMessenger` 由 `SimulationManager` 持有，`DetectorMessenger` 由 `DetectorConstruction` 持有，默认运行时 `/AIHL/app/...` 和 `/AIHL/detector/...` 可用。

建议：

```
AppMessenger：
    由 SimulationManager 持有；
    用于 /AIHL/app/printSummary、setVerbose 等轻量命令。

DetectorMessenger：
    由 DetectorConstruction 持有；
    用于 printRegistry、printSensitiveVolumes、printBiasVolumes、enableSD 等诊断命令。
```

注意：运行中修改 SD 状态不能简单生效，必须提示需要 `/run/reinitializeGeometry` 或重新初始化。

------

## P1.3 实现 `trap` / `trapezoid`

Round 2 已完成：`trd/trapezoid` 使用 `G4Trd`，`trap` 使用完整 `G4Trap`。下面的参数示例保留为配置参考；`trap` 仍需满足 Geant4 对 trap solid 的面共面等几何约束。

建议分两种：

```
shape = trd
dx1 = 5 mm
dx2 = 10 mm
dy1 = 4 mm
dy2 = 8 mm
dz = 2 mm
```

和完整：

```
shape = trap
dz = 2 mm
theta = 0 deg
phi = 0 deg
dy1 = 4 mm
dx1 = 5 mm
dx2 = 6 mm
alpha1 = 0 deg
dy2 = 8 mm
dx3 = 7 mm
dx4 = 8 mm
alpha2 = 0 deg
```

短期建议先做 `G4Trd`，再做完整 `G4Trap`。

------

## P1.4 补齐 `layered_device` 的 `copyNo` 和 metadata

Round 2 已完成：`layered_device` 已读取 `copyNo`，并把 `metadata.*` 与未知 key 透传到 `VolumeNode::userProperties`；`copyNo` 会由 `VolumeBuilder` 传给 `G4PVPlacement`。metadata 仍不会自动进入 `HitRecord` 或 CSV。

建议最小实现：

```
LayeredDeviceTemplate：
    对每个 [layer.*]：
        读取 copyNo；
        保存 metadata.*；
        保存未知 key 到 userProperties；
        排除 thickness、xy、material、position、rotation、sensitive、bias、region、cuts、visual 等已知 key。
```

这对后续 hit 输出、层编号、器件阵列分析非常有价值。

------

## P1.5 多线程输出最小修复

审计认为这是 P1，而且是结果可信度问题。当前 `_t0` 后缀存在，但 `OutputManager` / `ScoringManager` 共享，`threadId` 固定为 0，多线程会有 race 风险。

我建议不要一步到位切到 `G4AnalysisManager`。先做：

```
方案 A：per-thread OutputManager + per-thread ScoringManager + 外部 merge
```

第一步目标：

```
1. 单线程保持完全兼容；
2. 多线程时每个 worker 写 *_tN.csv；
3. master 不直接写 worker event 数据；
4. run 结束后提示 merge 命令；
5. 暂不自动 merge。
```

这比加锁写单文件更安全，也更容易调试。

------

# 四、P2：中期核心能力

P2 不建议现在立刻全部做，但应该作为下一轮开发主线。

------

## P2.1 `volume_fraction`

审计判断：parser 已经识别，但 `MaterialFactory` 后端明确抛异常。实现可行，但不能把体积分数误当质量分数。

建议实现规则：

```
仅允许 component 是 material；
用 rho_mix = sum(v_i * rho_i)；
用 w_i = v_i * rho_i / rho_mix；
最后调用 AddMaterial(subMaterial, w_i)。
```

不要允许元素直接使用 `volume_fraction`，除非提供显式密度。

------

## P2.2 GDML world replacement

审计认为 GDML 是 medium-term core feature，比 STL 更值得做。当前 `GDMLTemplate` 只是 placeholder，`VolumeBuilder` 会抛异常。

我建议不要把 GDML 塞进 `VolumeBuilder`，而是走：

```
DetectorConstruction::Construct()
    if geometry mode == gdml:
        G4GDMLParser parser;
        parser.Read(file);
        world = parser.GetWorldVolume();
        GeometryRegistry 从 LogicalVolumeStore / PhysicalVolumeStore 注册名称；
        sensitive_volumes / bias_volumes 根据名称标记；
        return world;
```

这比把 GDML 转成 `VolumeNode` 更稳。

------

## P2.3 DoseScorer / FluenceScorer

审计指出：`DoseScorer` 和 `FluenceScorer` 当前是 no-op，`LETScorer` 只是读取未填充的 `LETcalc` 字段。

建议优先级：

```
先 Dose
再 Fluence
最后 LET
```

Dose 最小定义：

```
dose = weighted_edep / mass
```

需要 volume mass。第一版可以只支持：

```
每个 logical volume 的 total dose
```

Fluence 先明确为：

```
track-length fluence = sum(stepLength * weight) / volume
```

不要直接叫“fluence”而不说明定义。

LET 先不要做真实实现，先写 `docs/LET_DEFINITION.md`。

------

# 五、P3：未来扩展，暂缓

这些方向现在不建议做：

```
STL import
Qt UI
ParticleAggregator 高级分析
importance / weight-window / splitting / Russian roulette
完整 LET 物理
```

审计明确建议：STL 风险高、Qt UI 复杂度高、高级 biasing 与当前 XS 架构不是同一层，应推迟。

------

# 六、我建议的实际开发顺序

## 第 0 轮：安全修复

```
0.1 修 run_gdml.mac
0.2 更新未实现功能文档
0.3 更新中文手册中的 avoid-for-now 列表
0.4 新增 XS biasing validation 文档和 macro
```

目标：用户不会被误导。

------

## 第 1 轮：命令可用性补齐

```
1.1 OutputMessenger
1.2 AppMessenger 注册
1.3 DetectorMessenger 注册
```

目标：`/control/manual` 里看到真实可用命令。

------

## 第 2 轮：几何低风险增强

```
2.1 trd / trap
2.2 layered_device copyNo
2.3 layered_device metadata passthrough
2.4 增加 geometry smoke tests
```

目标：修掉 parser-only 几何功能。Round 2 已完成，可作为回归清单保留。

------

## 第 3 轮：多线程输出最小可用

```
3.1 worker-local OutputManager
3.2 worker-local ScoringManager
3.3 正确 thread id
3.4 每线程 CSV 输出
3.5 文档化 merge 流程
```

目标：多线程不再破坏输出。

------

## 第 4 轮：材料和 GDML

```
4.1 volume_fraction
4.2 GDML world replacement
4.3 GDML sensitive/bias volume name mapping
```

目标：补通中期核心能力。

------

## 第 5 轮：Scoring 物理量增强

```
5.1 DoseScorer
5.2 track-length FluenceScorer
5.3 LET 定义文档
5.4 LET prototype
```

目标：让 scoring 从“能量沉积”扩展到常用物理量。

------

# 七、建议给 Codex 的总任务提示词

你可以这样让 Codex 先做规划，不急着改代码：

```
请根据 IMPLEMENTATION_AND_FEASIBILITY_AUDIT.md 和当前源码，为 G4UniversalSim 制定后续开发计划。

要求：
1. 不要直接修改源码。
2. 先按 P0/P1/P2/P3 对功能排序。
3. 对每个功能说明：
   - 当前实现状态；
   - 调用链断点；
   - 是否容易误导用户；
   - Geant4 可行性；
   - 是否应该现在做；
   - 预计修改文件；
   - 最小实现范围；
   - 验证方法。
4. 重点处理：
   - run_gdml.mac 安全修正；
   - OutputMessenger；
   - AppMessenger / DetectorMessenger 注册；
   - trap/trapezoid；
   - layered_device copyNo/metadata；
   - 多线程输出；
   - volume_fraction；
   - GDML 真导入；
   - Dose/Fluence scorer。
5. 明确推迟：
   - STL；
   - Qt UI；
   - ParticleAggregator；
   - advanced biasing；
   - 真实 LET。
6. 输出：
   - 后续路线图；
   - 每一轮 Codex 任务清单；
   - 每一轮验收标准；
   - 不建议现在做的事项。
```

------

# 八、最终建议

你现在最应该做的不是继续写新大模块，而是把项目从：

```
功能很多，但部分是 placeholder/stub/parser-only
```

推进到：

```
用户看到的功能都能真实运行；
不能运行的功能明确标注；
单线程输出可信；
biasing 有 analog 对照验证；
多线程至少不会损坏输出。
```

推荐下一步从 **P0 + P1** 开始：

```
1. 修正文档和危险 macro；
2. OutputMessenger、AppMessenger、DetectorMessenger 已在 Round 1 完成；
3. trd/trap 与 layered_device copyNo/metadata 已在 Round 2 完成；
4. 下一步重点转向 per-thread 输出设计和修复。
```

这条路线最稳，也最符合当前审计报告的结论。
