# GDMLTemplate placeholder configuration.
# VolumeBuilder intentionally does not import GDML yet; this file is for template parsing only.

[gdml]
file = geometry/model.gdml
world_name = world
sensitive_volumes = SV1, SV2
bias_volumes = Shield1
region.SV1 = SensitiveRegion
region.Shield1 = ShieldRegion
