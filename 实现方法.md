# Geant4 通用仿真框架 AI 开发规划文档（简版）

本文档用于指导后续使用 AI 工具分模块开发一个 **配置驱动、模板几何、可扩展 Scoring/Biasing 的通用 Geant4 程序框架**。
参考对象是 G4SEE，但目标不是完全复刻 G4SEE，而是借鉴其成熟模块：主程序入口、几何构建、物理列表、截面偏置、Action 链、SensitiveDetector/Hit/Scoring 和后处理工具等。

------

# 1. 项目总体目标

本项目目标是实现一个通用 Geant4 仿真框架，使用户尽量通过：

```text
config/*.ini
macros/*.mac
```

完成常见仿真任务，而不是每次从零编写：

```text
DetectorConstruction
PhysicsList
PrimaryGeneratorAction
RunAction
EventAction
SteppingAction
SensitiveDetector
Scoring
Biasing
```

核心目标：

```text
1. 支持多种几何模板；
2. 支持 material.ini 外部材料定义；
3. 支持 GPS 粒子源；
4. 支持 Edep / Hit / LET / Dose 等计分扩展；
5. 支持截面偏置 XS bias；
6. 支持后续 GDML、阵列结构、屏蔽计算、MicroElec 等高级扩展；
7. 所有模块尽量低耦合，方便 AI 分模块开发。
```

------

# 2. 总体开发原则

## 2.1 接口先行，复杂实现后置

很多模块不能等到最后才出现，例如：

```text
BiasingManager
ScoringManager
OutputManager
GeometryRegistry
VolumeNode
ParticleHit
```

即使第一版不完整实现，也必须先保留接口。否则后面接入 Biasing、Scoring、Region、GDML 时会大面积返工。

------

## 2.2 不要把所有逻辑塞进 DetectorConstruction

G4SEE 的 `DeviceConstruction` 同时承担材料、几何、region/cuts、bias volume 等功能。我们的项目应拆分为：

```text
MaterialManager      → 负责材料
GeometryManager      → 负责几何模板和构造
GeometryRegistry     → 负责 volume 名称索引
BiasingManager       → 负责偏置配置和 operator 绑定
ScoringManager       → 负责计分
DetectorConstruction → 只作为 Geant4 几何入口
```

------

## 2.3 开发顺序核心逻辑

推荐顺序：

```text
基础工具
→ 配置与输出
→ 核心接口预留
→ 材料与几何数据
→ Geant4 几何闭环
→ Physics / Source / Actions
→ Hits / Scoring
→ Biasing 真实实现
→ 高级功能
```

------

# 3. 推荐文件结构

```text
G4UniversalSim/
│
├── main.cc
├── CMakeLists.txt
│
├── include/
│   ├── Core/
│   │   ├── SimulationContext.hh
│   │   ├── SimulationManager.hh
│   │   └── AppMessenger.hh
│   │
│   ├── Config/
│   │   ├── IniReader.hh
│   │   ├── ConfigManager.hh
│   │   └── ConfigValue.hh
│   │
│   ├── Utils/
│   │   ├── StringUtils.hh
│   │   ├── FileUtils.hh
│   │   ├── UnitParser.hh
│   │   └── G4NameUtils.hh
│   │
│   ├── Output/
│   │   ├── OutputManager.hh
│   │   ├── OutputMessenger.hh
│   │   ├── CsvWriter.hh
│   │   ├── Histogram1D.hh
│   │   ├── Histogram2D.hh
│   │   └── RunSummary.hh
│   │
│   ├── Materials/
│   │   ├── MaterialManager.hh
│   │   ├── MaterialMessenger.hh
│   │   ├── MaterialIniReader.hh
│   │   └── MaterialFactory.hh
│   │
│   ├── Geometry/
│   │   ├── VolumeNode.hh
│   │   ├── GeometryConfig.hh
│   │   ├── GeometryRegistry.hh
│   │   ├── GeometryManager.hh
│   │   ├── VolumeBuilder.hh
│   │   └── GeometryUtils.hh
│   │
│   ├── Templates/
│   │   ├── GeometryTemplate.hh
│   │   ├── SimpleBoxTemplate.hh
│   │   ├── LayeredDeviceTemplate.hh
│   │   ├── HierarchicalVolumeTemplate.hh
│   │   ├── ArrayTemplate.hh
│   │   ├── ShieldingTemplate.hh
│   │   └── GDMLTemplate.hh
│   │
│   ├── Detector/
│   │   ├── DetectorConstruction.hh
│   │   └── DetectorMessenger.hh
│   │
│   ├── Physics/
│   │   ├── PhysicsList.hh
│   │   ├── PhysicsManager.hh
│   │   ├── PhysicsMessenger.hh
│   │   ├── PhysicsFactory.hh
│   │   ├── MicroElecPhysics.hh
│   │   └── ElectronCapture.hh
│   │
│   ├── Source/
│   │   ├── PrimaryGeneratorAction.hh
│   │   ├── SourceManager.hh
│   │   └── SourceMessenger.hh
│   │
│   ├── Actions/
│   │   ├── ActionInitialization.hh
│   │   ├── RunAction.hh
│   │   ├── EventAction.hh
│   │   ├── SteppingAction.hh
│   │   └── TrackingAction.hh
│   │
│   ├── Hits/
│   │   ├── ParticleHit.hh
│   │   ├── ParticleHitCollection.hh
│   │   ├── SensitiveDetector.hh
│   │   └── TrackInformation.hh
│   │
│   ├── Scoring/
│   │   ├── ScoringManager.hh
│   │   ├── ScoringMessenger.hh
│   │   ├── ScorerBase.hh
│   │   ├── EdepScorer.hh
│   │   ├── LETScorer.hh
│   │   ├── DoseScorer.hh
│   │   ├── FluenceScorer.hh
│   │   └── ParticleAggregator.hh
│   │
│   └── Biasing/
│       ├── BiasingConfig.hh
│       ├── BiasingManager.hh
│       ├── BiasingMessenger.hh
│       ├── BiasingXS.hh
│       ├── BiasingMultiParticleXS.hh
│       ├── BiasingRegion.hh
│       └── BiasingImportance.hh
│
├── src/
│   ├── Core/
│   ├── Config/
│   ├── Utils/
│   ├── Output/
│   ├── Materials/
│   ├── Geometry/
│   ├── Templates/
│   ├── Detector/
│   ├── Physics/
│   ├── Source/
│   ├── Actions/
│   ├── Hits/
│   ├── Scoring/
│   └── Biasing/
│
├── config/
│   ├── main.ini
│   ├── materials/material.ini
│   ├── geometry/simple_box.ini
│   ├── geometry/layered_device.ini
│   ├── geometry/hierarchical_volume.ini
│   ├── geometry/array.ini
│   ├── geometry/shielding.ini
│   ├── geometry/gdml_import.ini
│   ├── physics/default.ini
│   ├── source/proton_beam.ini
│   ├── scoring/default.ini
│   └── biasing/xs_bias.ini
│
├── macros/
│   ├── run_simple.mac
│   ├── run_layered.mac
│   ├── run_array.mac
│   ├── run_hierarchical.mac
│   ├── run_shielding.mac
│   ├── run_gdml.mac
│   ├── run_biasing.mac
│   └── vis.mac
│
├── examples/
│   ├── 01_simple_box/
│   ├── 02_layered_device/
│   ├── 03_hierarchical_volume/
│   ├── 04_array_detector/
│   ├── 05_shielding/
│   └── 06_gdml_import/
│
├── tools/
│   └── mergeHistograms.cc
│
├── scripts/
│   ├── plot_hist.py
│   ├── merge_outputs.py
│   └── scan_params.py
│
└── output/
```

------

# 4. 模块任务、接口与目标

## 4.1 Utils 模块

### 保留接口

```text
StringUtils::Trim()
StringUtils::Split()
StringUtils::ToLower()
StringUtils::RemoveComment()

FileUtils::Exists()
FileUtils::CreateDirectories()
FileUtils::CanonicalPath()

UnitParser::ParseLength()
UnitParser::ParseEnergy()
UnitParser::ParseDoubleWithUnit()
```

### 任务

```text
1. 提供字符串处理；
2. 提供文件路径处理；
3. 提供基础单位解析；
4. 为 Config、Materials、Geometry、Output 服务。
```

### 目标

无 Geant4 依赖，可单独测试。

### 实现顺序

第 1 批。

------

## 4.2 Config 模块

### 保留接口

```text
IniReader::Load(filename)
IniReader::HasSection(section)
IniReader::HasKey(section, key)
IniReader::GetString(section, key)

ConfigManager::LoadMainConfig(filename)
ConfigManager::GetString(section, key, defaultValue)
ConfigManager::GetDouble(section, key, defaultValue)
ConfigManager::GetBool(section, key, defaultValue)
ConfigManager::GetVector(section, key)
```

### 任务

```text
1. 读取 ini 文件；
2. 管理多个配置文件；
3. 提供统一配置访问接口；
4. 为材料、几何、源、物理、计分、偏置提供参数。
```

### 目标

后续模块不直接解析 ini，只通过 `ConfigManager` 读取配置。

### 实现顺序

第 1 批。

------

## 4.3 Output 模块

### 保留接口

```text
CsvWriter::Open()
CsvWriter::WriteHeader()
CsvWriter::WriteRow()
CsvWriter::Close()

Histogram1D::Fill(value, weight)
Histogram1D::WriteCSV(filename)
Histogram1D::Merge(other)

OutputManager::SetOutputDir()
OutputManager::WriteHit()
OutputManager::WriteEventEdep()
OutputManager::WriteHistogram()
OutputManager::WriteRunSummary()
```

### 任务

```text
1. 管理输出目录；
2. 输出 hits_t0.csv；
3. 输出 event_edep_t0.csv；
4. 输出 hist_edep_t0.csv；
5. 输出 run_summary.txt；
6. 为多线程输出预留 thread id。
```

### 目标

统一输出，不让 RunAction / EventAction / Scoring 直接管理文件细节。

### 实现顺序

第 1～2 批。

------

## 4.4 Core 模块

### 保留接口

```text
SimulationContext::SetOutputDir()
SimulationContext::SetSeed()
SimulationContext::SetNumThreads()
SimulationContext::SetMainConfig()

SimulationManager::Initialize()
SimulationManager::LoadConfig()
SimulationManager::GetMaterialManager()
SimulationManager::GetGeometryManager()
SimulationManager::GetBiasingManager()
SimulationManager::GetScoringManager()
```

### 任务

```text
1. 保存全局状态；
2. 管理各 Manager 的生命周期；
3. 协调配置加载；
4. 给 main.cc 提供统一入口。
```

### 目标

`main.cc` 尽量简洁，不直接操作所有模块。

### 实现顺序

第 2 批。

------

## 4.5 Materials 模块

### 保留接口

```text
MaterialIniReader::Load(filename)

MaterialManager::LoadMaterials(filename)
MaterialManager::GetMaterial(name)
MaterialManager::HasMaterial(name)
MaterialManager::BuildNistMaterial(name)
MaterialManager::BuildCustomMaterial(desc)
MaterialManager::PrintMaterials()
```

### 任务

```text
1. 支持 G4_NIST 材料；
2. 支持 material.ini；
3. 支持元素、材料、混合物；
4. 后续支持同位素材料。
```

### 目标

几何构建时只通过 `MaterialManager::GetMaterial()` 获取材料。

### 实现顺序

第 3 批。

------

## 4.6 Geometry 数据模块

### 保留接口

```text
VolumeNode::name
VolumeNode::parentName
VolumeNode::shape
VolumeNode::materialName
VolumeNode::size
VolumeNode::position
VolumeNode::rotation
VolumeNode::sensitive
VolumeNode::bias
VolumeNode::regionName
VolumeNode::productionCuts
VolumeNode::children

GeometryRegistry::RegisterLogicalVolume(name, lv)
GeometryRegistry::GetLogicalVolume(name)
GeometryRegistry::MarkSensitiveVolume(name)
GeometryRegistry::MarkBiasVolume(name)
GeometryRegistry::GetSensitiveLogicalVolumes()
GeometryRegistry::GetBiasLogicalVolumes()
```

### 任务

```text
1. 用 VolumeNode 描述几何；
2. 记录 volume 的 sensitive/bias/region/cuts 信息；
3. 用 GeometryRegistry 统一管理 G4LogicalVolume 名称映射；
4. 为 SensitiveDetector、Biasing、Region、GDML 预留接口。
```

### 目标

几何模板不直接创建复杂 Geant4 逻辑，而是先生成 `VolumeNode` 树。

### 实现顺序

第 2～3 批。

------

## 4.7 Templates 模块

### 保留接口

```text
GeometryTemplate::BuildNodes(config)
GeometryTemplate::Name()

SimpleBoxTemplate::BuildNodes(config)
LayeredDeviceTemplate::BuildNodes(config)
HierarchicalVolumeTemplate::BuildNodes(config)
ArrayTemplate::BuildNodes(config)
ShieldingTemplate::BuildNodes(config)
GDMLTemplate::BuildNodes(config)
```

### 任务

```text
1. SimpleBoxTemplate：最小测试模型；
2. LayeredDeviceTemplate：核心多层器件模板；
3. HierarchicalVolumeTemplate：parent-child 体积树；
4. ArrayTemplate：阵列结构；
5. ShieldingTemplate：屏蔽结构；
6. GDMLTemplate：导入 GDML。
```

### 目标

第一版重点完成：

```text
SimpleBoxTemplate
LayeredDeviceTemplate
```

其他模板先保留接口。

### 实现顺序

第 3 批。

------

## 4.8 GeometryManager / VolumeBuilder

### 保留接口

```text
GeometryManager::SetTemplate(name)
GeometryManager::LoadGeometryConfig(filename)
GeometryManager::BuildWorld()
GeometryManager::GetRegistry()

VolumeBuilder::BuildWorld(rootNode)
VolumeBuilder::BuildVolume(node, motherLogical)
VolumeBuilder::CreateSolid(node)
VolumeBuilder::CreateRegion(node, logicalVolume)
```

### 任务

```text
1. 选择几何模板；
2. 从配置生成 VolumeNode；
3. 创建 G4Solid；
4. 创建 G4LogicalVolume；
5. 创建 G4PVPlacement；
6. 设置 region / cuts；
7. 向 GeometryRegistry 注册 volume；
8. 标记 sensitive 和 bias volume。
```

### 目标

完成：

```text
VolumeNode → Geant4 几何
```

### 实现顺序

第 4 批。

------

## 4.9 Detector 模块

### 保留接口

```text
DetectorConstruction::Construct()
DetectorConstruction::ConstructSDandField()
DetectorConstruction::GetGeometryRegistry()

DetectorMessenger:
/sim/detector/setTemplate
/sim/detector/loadConfig
/sim/detector/checkOverlaps
```

### 任务

```text
1. 作为 Geant4 几何入口；
2. 调用 GeometryManager 构建 world；
3. 在 ConstructSDandField 中注册 SensitiveDetector；
4. 不直接解析材料和几何配置；
5. 不保存 bias 粒子参数。
```

### 目标

保持干净，只做 Geant4 接口层。

### 实现顺序

第 4 批。

------

## 4.10 Physics 模块

### 保留接口

```text
PhysicsList::ConstructParticle()
PhysicsList::ConstructProcess()
PhysicsList::SetCuts()
PhysicsList::ConfigureBiasingPhysics()

PhysicsManager::SetPhysicsList(name)
PhysicsManager::SetDefaultCut(value)
PhysicsManager::SetRegionCut(region, particle, cut)

PhysicsMessenger:
/sim/physics/setList
/sim/physics/setCut
/sim/physics/setRegionCut
```

### 任务

```text
1. 注册基础物理列表；
2. 设置 production cuts；
3. 为 region cuts 预留接口；
4. 为 biasing physics 预留 hook；
5. 后续支持 MicroElecPhysics / ElectronCapture。
```

### 目标

第一版完成标准物理列表和 cuts；bias hook 必须保留。

### 实现顺序

第 5 批。

------

## 4.11 Source 模块

### 保留接口

```text
PrimaryGeneratorAction::GeneratePrimaries(event)

SourceManager::UseGPS()
SourceManager::LoadSourceConfig(filename)
SourceManager::ApplyPreset(name)

SourceMessenger:
/sim/source/preset
/sim/source/loadConfig
```

### 任务

```text
1. 底层使用 G4GeneralParticleSource；
2. 兼容 Geant4 GPS 原生命令；
3. 提供常用源 preset；
4. 从 source.ini 读取简单源配置。
```

### 目标

不要重写复杂粒子源，优先复用 GPS。

### 实现顺序

第 5 批。

------

## 4.12 Actions 模块

### 保留接口

```text
ActionInitialization::BuildForMaster()
ActionInitialization::Build()

RunAction::BeginOfRunAction()
RunAction::EndOfRunAction()

EventAction::BeginOfEventAction()
EventAction::EndOfEventAction()

SteppingAction::UserSteppingAction()

TrackingAction::PreUserTrackingAction()
TrackingAction::PostUserTrackingAction()
```

### 任务

```text
1. ActionInitialization 注册所有 action；
2. RunAction 管理 run-level 输出；
3. EventAction 管理 event-level scoring；
4. SteppingAction 处理 step-level 逻辑；
5. TrackingAction 写入 TrackInformation。
```

### 目标

Action 只调用 Manager，不直接耦合具体 scorer 或文件输出。

### 实现顺序

第 6 批。

------

## 4.13 Hits 模块

### 保留接口

```text
ParticleHit:
SetEventID()
SetTrackID()
SetParentID()
SetName()
SetEdep()
SetNdep()
SetStepLength()
SetFirstPos()
SetLastPos()
SetMomentum()
SetProcess()
SetVolume()
SetWeight()
SetLETcalc()
SetLETstep()

SensitiveDetector::Initialize()
SensitiveDetector::ProcessHits()
SensitiveDetector::EndOfEvent()
```

### 任务

```text
1. ParticleHit 存储 step/hit 数据；
2. SensitiveDetector 从 G4Step 创建 ParticleHit；
3. 记录 edep、ndep、position、momentum、process、volume、weight；
4. 为 LET、Dose、ParticleGrouping 预留字段。
```

### 目标

Hit 数据结构一次设计好，避免后续 LET/Bias/Grouping 反复修改。

### 实现顺序

第 7 批。

------

## 4.14 Scoring 模块

### 保留接口

```text
ScorerBase::BeginRun()
ScorerBase::EndRun()
ScorerBase::BeginEvent()
ScorerBase::EndEvent()
ScorerBase::ScoreHit(hit)
ScorerBase::Write(outputManager)

EdepScorer::ScoreHit(hit)

ScoringManager::RegisterScorer()
ScoringManager::BeginRun()
ScoringManager::EndRun()
ScoringManager::BeginEvent()
ScoringManager::EndEvent()
ScoringManager::ScoreHit(hit)
```

### 任务

```text
1. 管理多个 scorer；
2. 实现 EdepScorer；
3. 支持 raw_edep；
4. 支持 weighted_edep；
5. 输出 event_edep 和 hist_edep；
6. 后续扩展 LET、Dose、Fluence。
```

### 目标

Scoring 与 SensitiveDetector 解耦，SensitiveDetector 只负责产生 hit。

### 实现顺序

第 8 批。

------

## 4.15 Biasing 模块

### 保留接口

```text
BiasingConfig / XSBiasRule:
particleName
processNames
factor
onlyPrimary
minWeight
maxInteractions
volumeNames

BiasingManager::Enable()
BiasingManager::AddXSBiasParticle()
BiasingManager::AddXSBiasProcess()
BiasingManager::SetXSBiasFactor()
BiasingManager::AddBiasVolume()
BiasingManager::GetXSBiasRules()
BiasingManager::AttachOperators(registry)

BiasingMultiParticleXS::AddParticle()
BiasingMultiParticleXS::ExtendBiasForNonPrimaries()
BiasingMultiParticleXS::StartTracking()

BiasingXS::SetXSBiasFactor()
BiasingXS::StartRun()
BiasingXS::ProposeOccurenceBiasingOperation()
```

### 任务

```text
1. 统一保存 bias 配置；
2. 不把 bias 参数写入 DetectorConstruction；
3. PhysicsList 从 BiasingManager 获取 particle/process；
4. GeometryRegistry 提供需要 attach 的 logical volume；
5. BiasingManager 负责创建并 attach operator；
6. BiasingXS 使用 G4BOptnChangeCrossSection 实现 XS bias；
7. Scoring 使用 track weight 修正统计。
```

### 目标

实现比 G4SEE 更解耦的 biasing 流程。

### 实现顺序

接口第 2 批；真实实现第 9 批。

------

## 4.16 高级物理与后处理

### 保留接口

```text
MicroElecPhysics::ConstructParticle()
MicroElecPhysics::ConstructProcess()

ElectronCapture::PostStepGetPhysicalInteractionLength()
ElectronCapture::PostStepDoIt()

mergeHistograms(inputDir, outputFile)
```

### 任务

```text
1. MicroElecPhysics：区域微电子物理；
2. ElectronCapture：低能电子捕获；
3. mergeHistograms：合并多线程/多次 run 直方图；
4. Python scripts：绘图、合并输出、参数扫描。
```

### 目标

作为第二阶段功能，不影响 24 小时 MVP。

### 实现顺序

第 10 批。

------

# 5. 修正后的开发顺序总表

```text
阶段 0：工程骨架
0.1 main.cc
0.2 CMakeLists.txt
0.3 空类和目录结构

阶段 1：基础工具与配置
1.1 StringUtils
1.2 FileUtils
1.3 UnitParser
1.4 CsvWriter
1.5 Histogram1D
1.6 IniReader
1.7 ConfigValue
1.8 ConfigManager
1.9 RunSummary
1.10 OutputManager 基础版

阶段 2：核心接口预留
2.1 SimulationContext
2.2 SimulationManager
2.3 BiasingConfig
2.4 BiasingManager 轻量接口
2.5 GeometryRegistry
2.6 ScoringManager 接口

阶段 3：材料与几何数据
3.1 MaterialIniReader
3.2 MaterialFactory
3.3 MaterialManager
3.4 VolumeNode
3.5 GeometryConfig
3.6 GeometryTemplate
3.7 SimpleBoxTemplate
3.8 LayeredDeviceTemplate

阶段 4：Geant4 几何闭环
4.1 VolumeBuilder
4.2 GeometryManager
4.3 DetectorConstruction
4.4 DetectorMessenger

阶段 5：Physics / Source
5.1 PrimaryGeneratorAction
5.2 SourceManager
5.3 SourceMessenger
5.4 PhysicsList 基础版
5.5 PhysicsManager
5.6 PhysicsMessenger

阶段 6：Action 运行链
6.1 ActionInitialization
6.2 RunAction
6.3 EventAction
6.4 SteppingAction
6.5 TrackingAction
6.6 TrackInformation

阶段 7：Hit 与 SensitiveDetector
7.1 ParticleHit
7.2 ParticleHitCollection
7.3 SensitiveDetector
7.4 ConstructSDandField 绑定 SD

阶段 8：Scoring 与 Output 闭环
8.1 ScorerBase
8.2 EdepScorer
8.3 ScoringManager 完整版
8.4 ScoringMessenger
8.5 OutputManager 完整版

阶段 9：Biasing 真实实现
9.1 BiasingMessenger
9.2 BiasingXS
9.3 BiasingMultiParticleXS
9.4 BiasingManager::AttachOperators()
9.5 PhysicsList::ConfigureBiasingPhysics()

阶段 10：高级扩展
10.1 LETScorer
10.2 DoseScorer
10.3 FluenceScorer
10.4 ParticleAggregator / ParticleGrouping
10.5 MicroElecPhysics
10.6 ElectronCapture
10.7 ArrayTemplate
10.8 HierarchicalVolumeTemplate
10.9 GDMLTemplate
10.10 BiasingRegion / BiasingImportance
10.11 mergeHistograms.cc
10.12 Python scripts
```

------

# 6. 24 小时 MVP 范围

24 小时内不建议做完全部模块。建议完成：

```text
必须完成：
1. main.cc / CMakeLists.txt
2. Utils
3. Config
4. Output 基础
5. MaterialManager
6. VolumeNode
7. GeometryRegistry
8. SimpleBoxTemplate
9. LayeredDeviceTemplate
10. VolumeBuilder
11. GeometryManager
12. DetectorConstruction
13. PhysicsList 基础版
14. PrimaryGeneratorAction
15. ActionInitialization
16. RunAction / EventAction / SteppingAction / TrackingAction
17. ParticleHit
18. SensitiveDetector
19. EdepScorer
20. ScoringManager

只保留接口：
1. BiasingManager
2. BiasingMessenger
3. BiasingXS
4. BiasingMultiParticleXS
5. LETScorer
6. DoseScorer
7. GDMLTemplate
8. ArrayTemplate
9. HierarchicalVolumeTemplate
```

MVP 成功标准：

```text
1. 工程可编译；
2. run_simple.mac 可运行；
3. run_layered.mac 可运行；
4. 可以从 material.ini 读取材料；
5. 可以构建多层 box 几何；
6. 可以绑定 sensitive volume；
7. 可以输出 hits_t0.csv；
8. 可以输出 event_edep_t0.csv；
9. ParticleHit 中已包含 weight；
10. Biasing 接口存在，后续可接入真实 XS bias。
```

------

# 7. 关键逻辑链

## 7.1 普通仿真链

```text
ConfigManager
    ↓
SimulationManager
    ↓
MaterialManager
    ↓
GeometryTemplate → VolumeNode
    ↓
VolumeBuilder → GeometryRegistry
    ↓
DetectorConstruction
    ↓
PhysicsList
    ↓
PrimaryGeneratorAction
    ↓
ActionInitialization
    ↓
SensitiveDetector → ParticleHit
    ↓
ScoringManager
    ↓
OutputManager
```

------

## 7.2 Biasing 链

```text
biasing.ini / macro
    ↓
BiasingManager 保存 XSBiasRule
    ↓
PhysicsList 配置 wrapped/biasing process
    ↓
GeometryRegistry 提供 bias logical volumes
    ↓
BiasingManager attach BiasingMultiParticleXS
    ↓
BiasingMultiParticleXS::AddParticle()
    ↓
BiasingXS::SetXSBiasFactor()
    ↓
G4BOptnChangeCrossSection
    ↓
ParticleHit.weight
    ↓
ScoringManager weighted scoring
```

------

## 7.3 Region / MicroElec 链

```text
VolumeNode.regionName / productionCuts
    ↓
VolumeBuilder 创建 G4Region
    ↓
PhysicsList / MicroElecPhysics 根据 regionName 配置物理模型
    ↓
ElectronCapture / MicroElec model 生效
```

------

# 8. AI 提示词模板

## 8.1 独立模块开发模板

```text
请为我的 Geant4 通用仿真框架开发【模块名】模块。

项目背景：
这是一个 C++17 + Geant4 项目，目标是构建一个配置驱动、模板几何、可扩展 scoring 和 biasing 的通用仿真框架。

文件要求：
1. 头文件放在 include/[模块目录]/。
2. 源文件放在 src/[模块目录]/。
3. 不要写 main.cc。
4. 不要实现不属于本模块的功能。

本次只开发：
【模块名】

模块职责：
[写明该模块负责什么]

需要实现的文件：
[列出 .hh 和 .cc]

需要实现的类和接口：
[列出类名和 public 方法]

依赖模块：
[列出依赖]

禁止事项：
[例如：不要 include Geant4；不要访问 DetectorConstruction；不要直接创建 RunManager]

错误处理要求：
1. 文件不存在时抛出 std::runtime_error。
2. 配置错误时输出包含 section/key 的信息。
3. 不要直接 std::exit。

测试要求：
请给出最小测试方法。

输出要求：
请输出完整可替换代码。
```

------

## 8.2 Geant4 接口模块开发模板

```text
请为我的 Geant4 通用仿真框架开发【模块名】模块。

项目要求：
1. 使用 C++17。
2. 尽量兼容 Geant4 10.7 和 Geant4 11.x。
3. 头文件放在 include/[模块目录]/。
4. 源文件放在 src/[模块目录]/。
5. 不要写 main.cc。
6. 不要实现其他模块的功能。

本模块职责：
[例如 DetectorConstruction 只负责调用 GeometryManager 构建 world，并在 ConstructSDandField 中绑定 SensitiveDetector]

需要实现的文件：
[列出文件]

依赖模块：
[列出依赖]

必须调用的外部接口：
[例如 GeometryManager::BuildWorld(), GeometryRegistry::GetSensitiveLogicalVolumes()]

生命周期要求：
1. 配置应在 /run/initialize 前完成。
2. Construct() 中只构建几何。
3. ConstructSDandField() 中只注册 SD/field。
4. 不要在运行中修改几何结构。

禁止事项：
1. 不要在本模块中解析 ini 文件。
2. 不要在本模块中直接创建材料。
3. 不要把 biasing 参数存储在 DetectorConstruction 中。
4. 不要把 scoring 逻辑写进 DetectorConstruction。

输出要求：
请输出完整 .hh 和 .cc。
并说明该模块在 Geant4 初始化流程中的调用时机。
```

------

## 8.3 Biasing 模块提示词模板

```text
请为我的 Geant4 通用仿真框架开发 Biasing 模块：【具体类名】。

项目目标：
实现类似 G4SEE 的 cross-section biasing，但结构上更解耦：
1. BiasingManager 统一保存 bias 配置；
2. PhysicsList 从 BiasingManager 读取 particle/process 配置；
3. GeometryRegistry 提供需要 attach bias operator 的 logical volumes；
4. BiasingMultiParticleXS 管理多个粒子的 BiasingXS；
5. BiasingXS 使用 G4BOptnChangeCrossSection 实现截面偏置；
6. Scoring 使用 track weight 进行 weighted scoring。

本次只实现：
【BiasingManager / BiasingMessenger / BiasingXS / BiasingMultiParticleXS】

需要实现的文件：
[列出文件]

接口要求：
[列出 public methods]

重要逻辑：
1. 不要把 bias 参数存到 DetectorConstruction。
2. 不要让 BiasingMessenger 同时写 PhysicsList 和 DetectorConstruction。
3. 所有 bias 配置统一写入 BiasingManager。
4. BiasingManager 负责根据 GeometryRegistry attach operator。
5. PhysicsList 负责注册 wrapped/biasing physics。
6. BiasingXS 负责实际 G4BOptnChangeCrossSection。

安全默认值：
1. onlyPrimary = true。
2. minWeight = 0.05。
3. maxInteractions = 5。
4. factor <= 0 时应报错。
5. particle/process 不存在时给出 G4Exception warning。

输出要求：
请输出完整可替换代码，并说明该类应该在哪个初始化阶段被调用。
```

------

## 8.4 Hit / Scoring 模块提示词模板

```text
请为我的 Geant4 通用仿真框架开发【Hit/Scoring 模块名】。

项目要求：
1. 使用 C++17。
2. 兼容 Geant4 10.7/11.x。
3. 支持未来 biasing，因此所有 hit/scoring 必须记录并使用 track weight。
4. 支持未来 LET/Dose/Fluence，因此 ParticleHit 中要预留必要字段。

本次实现：
[ParticleHit / SensitiveDetector / EdepScorer / ScoringManager]

需要实现的文件：
[列出文件]

ParticleHit 必须包含字段：
eventID
trackID
parentID
particleName
edep
ndep
stepLength
firstPosition
lastPosition
momentumDirection
kineticEnergy
processName
volumeName
weight
LETcalc
LETstep

Scoring 要求：
1. 统计 raw_edep。
2. 统计 weighted_edep = edep * weight。
3. 输出 event_edep.csv。
4. 支持 Histogram1D。
5. 不要直接依赖具体 DetectorConstruction。

输出要求：
请输出完整可替换代码，并说明如何与 EventAction/RunAction 对接。
```

------

## 8.5 接口预留模板

```text
请为我的 Geant4 通用仿真框架创建【模块名】的接口预留版本。

目标：
该模块暂时不实现复杂功能，但必须提供稳定接口，供其他模块提前依赖，避免后期返工。

需要实现的文件：
[列出 .hh/.cc]

接口必须包含：
[列出 class 和 public methods]

实现要求：
1. 方法可以先输出 warning 或返回默认值。
2. 不要抛出未实现异常，除非该方法被错误调用会导致物理结果错误。
3. 保证工程可编译。
4. 保证后续可以无痛替换为完整实现。

禁止：
1. 不要写 main。
2. 不要引入不必要依赖。
3. 不要实现其他模块逻辑。

输出：
请输出完整 .hh/.cc 代码。
```

------

# 9. 最终结论

本项目应采用：

```text
配置驱动
+ 模板几何
+ Manager 管理状态
+ Messenger 暴露命令
+ GeometryRegistry 管理体积
+ ScoringManager 管理计分
+ BiasingManager 管理偏置
```

最关键的设计点是：

```text
1. Biasing 接口必须提前，但真实偏置后实现；
2. ParticleHit 必须一开始包含 weight 和 LET 预留字段；
3. VolumeNode 必须一开始包含 sensitive / bias / region / cuts；
4. GeometryRegistry 必须早做；
5. OutputManager 必须早做，并预留多线程输出；
6. DetectorConstruction 不保存复杂业务状态；
7. PhysicsList 必须预留 bias hook 和 region hook；
8. Action 模块只调用 Manager，不直接写死具体输出逻辑。
```

按照这个文档开发，可以保证 AI 分模块生成代码时不会频繁推翻前面的接口。